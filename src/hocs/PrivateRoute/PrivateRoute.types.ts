import { ReactNode } from "react";
//import { BaseRoutesItem } from "../types/interfaces/routes";

//export type PrivateRouteProps = Pick<BaseRoutesItem, "denyShowLoginPage">;

export type PrivateRouteProps = {
	children: ReactNode
};
